# Copy to backend/.env and adjust. Used by local uvicorn and docker-compose.

# MySQL — local dev (adjust user/password/db). Docker Compose sets DATABASE_URL inside containers.
# If you expose MySQL on host port 3307 (see repo docker-compose.yml), use @127.0.0.1:3307
DATABASE_URL=mysql+pymysql://yelp:yelppass@127.0.0.1:3306/yelp_db

SECRET_KEY=replace-with-a-long-random-string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# AI assistant (restaurant service)
GEMINI_API_KEY=
TAVILY_API_KEY=

# Legacy README name — not read by ai_service (uses GEMINI_API_KEY)
GOOGLE_API_KEY=
